[pytest]
# Only collect the real test suite under tests/. The top-level *_test.py /
# benchmark*.py / tune_*.py / validate_*.py files are dev SCRIPTS (some run
# network/OCR code at import time) — they are NOT pytest tests.
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
